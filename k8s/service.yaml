apiVersion: v1
kind: Service
metadata:
  name: python-k8s-service
spec:
  type: NodePort
  selector:
    app: python-k8s-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
